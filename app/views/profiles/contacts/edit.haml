= render 'profiles/headers'

= render layout: 'layouts/profiles' do
  %section.profile_edit
    .col-lg-3= concept('profile/cell/edit', @model, user: @user, section: 'contact')
    .col-lg-9.change.well= render 'form'