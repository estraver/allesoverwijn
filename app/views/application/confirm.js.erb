var confirmFn = function(e) {
    var $closeBtn= $('form').find('button[formaction=close]');
    $closeBtn.data({params: {confirmed: true}});
    this.dialog.modal('hide');
    $closeBtn.click();
};

new App.Dialog('<%= @title %>', '<%= @message %>', confirmFn, function() {}).render();